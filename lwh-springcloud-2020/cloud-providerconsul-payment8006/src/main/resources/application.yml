server:
  port: 8006
spring:
  application:
    name: connsul-providerconsul-payment
### consul注册中心地址
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}